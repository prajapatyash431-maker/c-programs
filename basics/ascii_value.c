#include <stdio.h>

int main()
{
    // C program to print ASCII value of a character

    char a;
    printf("Enter character : ");
    scanf("%c", &a);

    printf("ASCII value of %c is : %d", a, a);

    return 0;
}