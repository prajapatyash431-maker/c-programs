#include <stdio.h>

int main()
{
    // Program to print flooting point value 1.0 - 10.0, Using loop.
    for (float i = 1.0; i <= 10.0; i++)
    {
        printf("%.1f \n", i);
    }

    return 0;
}